{
  "baseBranch": "origin/main",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test": {
      "outputMode": "new-only"
    },
    "validate": {
      "outputMode": "new-only"
    }
  }
}
